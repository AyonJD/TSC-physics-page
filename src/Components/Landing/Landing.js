import React from 'react';
import BannerContainer from './BannerContainer';

const Landing = () => {
    return (
        <div>
            <BannerContainer />
        </div>
    );
};

export default Landing;